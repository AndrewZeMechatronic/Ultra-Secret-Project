start;

input W, S, A, D; // input keys

int f, b, r, l; // control variables

// if one of input keys equals HIGH write input keys values into control variables
if(W || S || A || D = HIGH){ 
    f = W
    b = -S
    r = invsin(D)
    l = invsin(A)
}
else{
f = LOW
b = LOW
r = LOW
l = LOW
}

end;